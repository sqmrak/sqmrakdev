# Base URL the repository is served from, no trailing slash.
# Every absolute link in the metadata is built from this, so set it once here
# and re-run ./refresh.sh; nothing else needs editing.
REPO_URL="https://sqmrak.github.io/sqmrakdev"

REPO_ORIGIN="sqmrakdev"
REPO_LABEL="sqmrakdev"
REPO_DESCRIPTION="just another shitty repo :D"
REPO_MAINTAINER="sqmrak"

# the nightly channel is tinted apart so a tester can tell at a glance which
# storefront page they are looking at
REPO_DESCRIPTION_NIGHTLY="test builds. they break. report what you see"
REPO_TINT="#2f6bff"
REPO_TINT_NIGHTLY="#e0761b"
