# the site is generated and published to gh-pages; the build tooling stays local.
# published.json is the exception: it records which bytes went out under which
# version, and refresh.sh refuses to reuse a version or drop a published build
# on the strength of it
*

!.gitignore
!README.md
!published.json
